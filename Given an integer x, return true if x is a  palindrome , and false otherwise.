/**
 * @param {number} x
 * @return {boolean}
 */
var isPalindrome = function(x) {
    let val= x.toString();

let a= val.split("").reverse(" ").join("");

if(a == val){
return true;
}
else{
return false;
}

};
